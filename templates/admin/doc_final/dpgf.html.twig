{% extends 'base.html.twig' %}

{% block title %}DocFinal dpgf{% endblock %}

{% block body %}
    <div class="container">
        <h2>{{ produit[0] }}</h2>
        <h3>Complétez les informations nécessaires</h3>
        {{ form(form) }}

        <a class="btn btn-secondary" href="{{ path('cctp_show', {'id': cctp[0].id }) }}">Retour</a>
    </div>
{% endblock %}