<form method="post" action="{{ path('doc_final_delete', {'id': docFinal.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ docFinal.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
