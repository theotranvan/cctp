{% extends 'base.html.twig' %}

{% block title %}Mise à jour Utilisateur{% endblock %}

{% block body %}
    <h1>Mise à jour Utilisateur</h1>
    <div class="divButtonEditCctp">
        <a class="btn btn-primary" href="{{ path('utilisateur_index') }}">Retour liste Utilisateur</a>
        {{ include('admin/utilisateur/_delete_form.html.twig') }}
    <a class="btn btn-secondary" href="{{ path('admin_home') }}" role="button">Retour à l'accueil</a>
    </div>
    <div class="container">
        {{ include('admin/utilisateur/_form.html.twig', {'button_label': 'Valider'}) }}
    </div>
    
{% endblock %}
