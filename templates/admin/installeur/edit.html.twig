{% extends 'base.html.twig' %}

{% block title %}Mise à jour Installeur{% endblock %}

{% block body %}
    <h1>Mise à jour Installeur</h1>

    <div class="divButtonEditCctp">
    <a class="btn btn-primary" href="{{ path('installeur_index') }}">Retour liste Installeur</a>
        {{ include('admin/installeur/_delete_form.html.twig') }}
        <a class="btn btn-secondary" href="{{ path('admin_home') }}" role="button">Retour à l'acceuil</a>
    </div>

    <div class="container">
        {{ include('admin/installeur/_form.html.twig', {'button_label': 'Valider'}) }}
    </div>
{% endblock %}
