{% extends 'base.html.twig' %}

{% block title %}mise à jour Lignedevis{% endblock %}

{% block body %}
    <h1>Mise à jour Lignedevis</h1>

    <div class="divButton">
        <a class="btn btn-primary" href="{{ path('devis_show', {'id': lignedevi.devis.id}) }}">Retour Devis</a>
        {{ include('admin/lignedevis/_delete_form.html.twig') }}
    </div>

    <div class="container">
        {{ include('admin/lignedevis/_form.html.twig', {'button_label': 'Valider'}) }}
    </div>
{% endblock %}
