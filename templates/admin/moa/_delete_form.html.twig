<form method="post" action="{{ path('moa_delete', {'id': moa.id}) }}" onsubmit="return confirm('etes-vous sûrs de vouloir supprimer ce MOA?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ moa.id) }}">
    <button class="btn btn-outline-danger">Supprimer</button>
</form>
