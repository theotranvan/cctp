{% extends 'base.html.twig' %}

{% block title %}Créer nouveau Elément{% endblock %}

{% block body %}
    <h1>Créer nouveau Elément</h1>

    <div class="divButton">
        <a class="btn btn-primary" href="{{ path('produit_index') }}" role="button">Retour liste produits</a>
        <a class="btn btn-secondary" href="{{ path('admin_home') }}" role="button">Retour à l'accueil</a>
    </div>

    <div class="container">
        {{ include('admin/produit/_form.html.twig') }}
    </div>
    
{% endblock %}
