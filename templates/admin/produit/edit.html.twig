{% extends 'base.html.twig' %}

{% block title %}Mise à jour élément{% endblock %}

{% block body %}
    <h1>Mise à jour élément</h1>

    <div class="divButtonEditCctp">
        <a class="btn btn-primary" href="{{ path('produit_index') }}" role="button">Retour liste éléments</a>
        {{ include('admin/produit/_delete_form.html.twig') }}
        <a class="btn btn-secondary" href="{{ path('admin_home') }}" role="button">Retour à l'accueil</a>
    </div>

    <div class="container">
        {{ include('admin/produit/_form.html.twig', {'button_label': 'Valider'}) }}
    </div>

{% endblock %}
