{% extends 'base.html.twig' %}

{% block title %}Optionnel{% endblock %}

{% block body %}
    <h1>Optionnel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ optionnel.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ optionnel.content|raw }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('optionnel_index') }}">back to list</a>

    <a href="{{ path('optionnel_edit', {'id': optionnel.id}) }}">edit</a>

    {{ include('admin/optionnel/_delete_form.html.twig') }}
{% endblock %}
