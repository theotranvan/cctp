<form method="post" action="{{ path('optionnel_delete', {'id': optionnel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ optionnel.id) }}">
    <button class="btn">Delete</button>
</form>
