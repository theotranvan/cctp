{% extends 'base.html.twig' %}

{% block title %}Créer nouveau système{% endblock %}

{% block body %}
    <h1>Créer nouveau système</h1>

    <div class="divButton">
        <a class="btn btn-primary" href="{{ path('systeme_index') }}" role="button">Retour liste Système</a>
        <a class="btn btn-secondary" href="{{ path('admin_home') }}" role="button">Retour à l'accueil</a>
    </div>
    
    <div class="container">
        {{ include('admin/systeme/_form.html.twig') }}
    </div>
    
{% endblock %}


