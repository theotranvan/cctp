{% extends 'base.html.twig' %}

{% block title %}Mise à jour Systeme{% endblock %}

{% block body %}
    <h1>Mise à jour Systeme</h1>

    <div class="divButtonEditCctp">
        <a class="btn btn-primary" href="{{ path('systeme_index') }}" role="button">Retour liste Système</a>
        {{ include('admin/systeme/_delete_form.html.twig') }}
        <a class="btn btn-secondary" href="{{ path('admin_home') }}" role="button">Retour à l'accueil</a>
    </div>

    <div class="container">
        {{ include('admin/systeme/_form.html.twig', {'button_label': 'Valider'}) }}
    </div>
    
{% endblock %}
