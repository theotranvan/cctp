{% extends 'base.html.twig' %}

{% block title %}Mise à jour du Cctp{% endblock %}

{% block body %}
    <h1>Mise à jour du Cctp</h1>

    <div class="divButtonEditCctp">
        <a class="btn btn-primary" href="{{ path('cctp_index') }}" role="button">Retour liste Cctp</a>
        {{ include('admin/cctp/_delete_form.html.twig') }}
        <a class="btn btn-secondary" href="{{ path('admin_home') }}" role="button">Retour à l'accueil</a>
    </div>

    <div class="container">
        {{ include('admin/cctp/_form.html.twig', {'button_label': 'Valider'}) }}   
    </div>
     
{% endblock %}

