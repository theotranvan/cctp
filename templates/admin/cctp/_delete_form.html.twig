<form method="post" action="{{ path('cctp_delete', {'id': cctp.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce Cctp?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cctp.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
