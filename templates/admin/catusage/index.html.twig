{% extends 'base.html.twig' %}
{% block title %}Catégorie d'usages{% endblock %}
{% block page_title %}<i class="bi bi-tag me-2 text-primary"></i>Catégorie d'usages{% endblock %}
{% block body %}
<div class="d-flex justify-content-between align-items-center mb-4">
<nav aria-label="breadcrumb"><ol class="breadcrumb mb-0"><li class="breadcrumb-item"><a href="{{ path('admin_dashboard') }}">Accueil</a></li><li class="breadcrumb-item active">Catégorie d'usages</li></ol></nav>
<a href="{{ path('catusage_new') }}" class="btn btn-primary"><i class="bi bi-plus-circle me-1"></i>Nouveau</a>
</div>
<div class="card"><div class="card-body p-0">
{% if catusages is empty %}
<div class="text-center py-5 text-muted"><i class="bi bi-tag display-4 d-block mb-3 opacity-25"></i><p>Aucun Catégorie d'usage trouvé.</p><a href="{{ path('catusage_new') }}" class="btn btn-primary">Créer</a></div>
{% else %}
<div class="table-responsive"><table class="table table-hover mb-0">
<thead><tr><th>Nom</th><th class="text-end">Actions</th></tr></thead>
<tbody>{% for catusage in catusages %}<tr>
<td class="fw-semibold">{{ catusage.nomCatusage }}</td>
<td class="text-end"><div class="btn-group btn-group-sm">
<a href="{{ path('catusage_edit', {id: catusage.id}) }}" class="btn btn-outline-secondary"><i class="bi bi-pencil"></i></a>
<button type="button" class="btn btn-outline-danger" onclick="if(confirm('Supprimer ?')) document.getElementById('del-catusage-{{ catusage.id }}').submit()"><i class="bi bi-trash"></i></button>
</div>
<form id="del-catusage-{{ catusage.id }}" method="post" action="{{ path('catusage_delete', {id: catusage.id}) }}" class="d-none"><input type="hidden" name="_token" value="{{ csrf_token('delete' ~ catusage.id) }}"></form>
</td></tr>{% endfor %}</tbody>
</table></div>
{% endif %}
</div></div>
{% endblock %}
