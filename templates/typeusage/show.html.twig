{% extends 'base.html.twig' %}

{% block title %}Typeusage{% endblock %}

{% block body %}
    <h1>Typeusage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeusage.id }}</td>
            </tr>
            <tr>
                <th>Nom_usage</th>
                <td>{{ typeusage.nomUsage }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ typeusage.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_typeusage_index') }}">back to list</a>

    <a href="{{ path('app_typeusage_edit', {'id': typeusage.id}) }}">edit</a>

    {{ include('typeusage/_delete_form.html.twig') }}
{% endblock %}
